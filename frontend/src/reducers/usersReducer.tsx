const initialUsersState = {
	items: [], // Array of users
	loading: false,
	error: null,
};

export const usersReducer = (state = initialUsersState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
